Tests for `attrappe.device`: the instrument model, its state, the error queue
and the status registers.

Empty until #23 and #24 land. The directory exists so the first test of the
instrument model arrives in the layout rather than inventing one.
