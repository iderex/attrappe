Tests for `attrappe.scpi`: the message parser, the command tree and the
dispatch.

Empty until #21 and #22 land. The directory exists so the first parser test
arrives in the layout rather than inventing one.
