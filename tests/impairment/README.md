Tests for `attrappe.impairment`: the reading pipeline, the physical stages and
the fault schedule.

Empty until the physical-plausibility milestone lands. The stages are the
tests that need the manual clock and a seeded stream most, which is what
`tests/conftest.py` owes them and does not yet carry.
